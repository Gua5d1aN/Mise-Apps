# Supabase connection
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Organisation UUID — from running phase2_schema.sql in Supabase SQL Editor
VITE_ORG_ID=your-org-uuid-here

# Admin password gate REMOVED in Phase 3.
# Admin access is now controlled by per-admin PINs in the admin_accounts table.
# No env var needed.
