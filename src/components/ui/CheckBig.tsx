/**
 * CheckBig — large checkmark for the success and issueSuccess screens.
 * Sits inside a teal circle, so stroke is dark navy.
 * @author Joshua Bosen
 */
export function CheckBig() {
  return (
    <svg width="38" height="38" viewBox="0 0 38 38" fill="none" aria-hidden="true" focusable="false">
      <path d="M8 19l8 8L30 11" stroke="#0C0A22" strokeWidth="3.5" strokeLinecap="round" strokeLinejoin="round" />
    </svg>
  );
}
